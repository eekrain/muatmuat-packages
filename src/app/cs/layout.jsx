import MainLayout from "@/layout/CS/MainLayout/MainLayout";

export const metadata = {
  title: "Muatrans CS",
  description: "",
};

export default function CSLayout({ children }) {
  return <MainLayout>{children}</MainLayout>;
}
